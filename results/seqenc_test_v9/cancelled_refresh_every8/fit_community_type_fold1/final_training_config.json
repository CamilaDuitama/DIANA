{
  "features_path": "data/matrices/matrix_v9_train/unitigs.frac.mat",
  "metadata_path": "data/splits_v9/train_metadata.tsv",
  "train_ids_path": "results/calibration_v9/ids/cal_train_fold1.txt",
  "output_dir": "results/seqenc_test_v9/fit_community_type_fold1",
  "hyperparameters": {
    "model_params": {
      "hidden_dims": [
        128,
        320,
        384,
        64
      ],
      "dropout": 0.1327911346886859,
      "activation": "gelu",
      "use_batch_norm": false
    },
    "trainer_params": {
      "learning_rate": 0.005281540154746062,
      "weight_decay": 2.71118920398746e-06,
      "task_weights": {
        "community_type": 1.0
      }
    },
    "batch_size": 128
  },
  "task_names": [
    "community_type"
  ],
  "task_types": {
    "community_type": "classification"
  },
  "task_weights": {
    "community_type": 1.0
  },
  "label_smoothing_per_task": {
    "community_type": 0.12404290975543691
  },
  "validation_split": 0.1,
  "max_epochs": 200,
  "early_stopping_patience": 20,
  "random_seed": 42,
  "class_imbalance": {
    "method": "logit_adjustment",
    "logit_adjust_tau": 0.6441734529984019,
    "search_tau": false,
    "class_weights": null,
    "_comment": "tau is SEARCHED in [0.05, 0.8]. tau=1.0, the parameter-free default, over-corrects at this imbalance: dev fold 2 single-task `feature` scored 0.009 at tau=1.0 against 0.484 at tau=0.25.",
    "_selected_by": "grouped Optuna search over the dev folds"
  },
  "_provenance": {
    "source": "results/search_v9_final_single_community_type/cv_results/search_all_train_best_params.json",
    "selection": "single Optuna search over all of train, dev folds as inner CV",
    "why": "per-outer-fold hyperparameters would be selected on fold difficulty; fold 4 scored 0.453 against 0.154-0.238 because it had 98.8 % in-vocabulary coverage and a 75.3 % majority class",
    "representation": "data/matrices/matrix_v9_train/unitigs.frac.mat",
    "hyperparameters_copied_from": "results/kmer_test_v9/configs/kmer_community_type_fold1.json",
    "card_length": "hidden_dims[0] = 128, read off the replaced layer rather than configured"
  },
  "_comment": "S4: the input layer's 110,202 private per-unitig weight vectors are replaced by vectors computed from each unitig's bases by one small CNN shared across the whole vocabulary, with two gates per unitig (one scaled by the fraction, one by presence). The feature matrix, folds, ids, hyperparameters and every layer after the first are unchanged from the S1/calibration arms, so `results/calibration_v9` is the paired fraction baseline on exactly these folds. Score with 26_pooled_representation_test.py. A gain here is only a SEQUENCE result if it does not also appear in the shuffled arm (results/seqenc_shuf_v9), which preserves each unitig's base composition and length and destroys only order; a gain in both arms is a capacity or regularisation effect and must be reported as one. Held-out untouched.",
  "sequence_encoder": {
    "bases": "data/sequences_v9/unitig_bases.npy",
    "offsets": "data/sequences_v9/unitig_offsets.npy",
    "refresh_every": 8,
    "max_padded_per_chunk": 1000000,
    "embed_dim": 32,
    "channels": [
      96,
      96
    ],
    "kernel": 9,
    "reader_activation": "relu"
  }
}